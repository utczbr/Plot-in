from enum import Enum

class ChartType(Enum):
    BAR = "bar"
    LINE = "line"
    SCATTER = "scatter"
    BOX = "box"
    PIE = "pie"
    AREA = "area"
    HISTOGRAM = "histogram"
    HEATMAP = "heatmap"
