# Chart Analysis System v1.0.0
# Generated: 2025-11-09
# Python: >=3.10,<3.14

# Core Dependencies
numpy==1.26.4; python_version >= "3.8"
opencv-python==4.8.1.78
# macOS arm64 now uses mainline onnxruntime.
onnxruntime==1.21.1
scikit-learn==1.3.2

# Machine Learning
scipy==1.11.4
# hdbscan: on macOS, Xcode Command Line Tools are required to build from source.
# scikit-learn >=1.3 provides sklearn.cluster.HDBSCAN as a compiler-free alternative.
# Uncomment only if you have Xcode CLT and prefer the standalone package.
# hdbscan==0.8.33
optuna==3.4.0
matplotlib==3.7.5
tqdm==4.66.4

# OCR Engines
easyocr==1.7.1
# Constrain torch pulled by EasyOCR: tested range for macOS arm64/x86_64 wheels.
# Upper bound tracks EasyOCR 1.7.x compat; bump after testing with newer versions.
torch>=2.1,<2.6

# Data Processing
pandas==2.1.4
Pillow==10.1.0

# Document Processing
PyMuPDF==1.23.8
PyYAML==6.0.1
doclayout_yolo==0.0.3

# GUI
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip==13.6.0

# Development Tools (separate file)
# See requirements-dev.txt

# Known Issues:
# - EasyOCR requires CUDA 11.x for GPU support
# - PyMuPDF may require additional system libraries on Linux
# - ONNX Runtime GPU requires CUDA 11.x and cuDNN 8.x
