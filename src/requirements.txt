# Chart Analysis System v1.0.0
# Generated: 2025-11-09
# Python: >=3.10,<3.14

# Core Dependencies
numpy==1.26.4; python_version >= "3.8"
opencv-python==4.8.1.78
# macOS arm64 now uses mainline onnxruntime.
onnxruntime==1.21.1


# Machine Learning
scikit-learn==1.3.2
scipy==1.11.4
# hdbscan: the standalone package (hdbscan==0.8.x) requires a C++ compiler on
# Windows. scikit-learn >=1.3 ships sklearn.cluster.HDBSCAN which is used as
# the primary backend (see clustering.py). The line below is intentionally
# commented out; users who want the standalone package can install it manually
# after installing Microsoft C++ Build Tools.
# hdbscan==0.8.33

# Deep Learning
# torch+cpu must be listed BEFORE easyocr so pip resolves easyocr's torch
# dependency against the CPU wheel rather than the default CUDA build.
# On Windows/Linux without a GPU the CUDA wheel fails with:
#   OSError: [WinError 1114] A dynamic link library (DLL) initialization routine failed.
# The CPU wheel has no CUDA/cuDNN dependency and works everywhere.
torch==2.1.2+cpu; sys_platform != "darwin"
torchvision==0.16.2+cpu; sys_platform != "darwin"

# OCR Engines
easyocr==1.7.1
# PaddleOCR: uses ONNX Runtime models, no paddlepaddle dependency required.

# Data Processing
Pillow==10.1.0

# Document Processing
PyMuPDF==1.23.8
PyYAML==6.0.1
doclayout_yolo==0.0.3

# GUI
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip==13.6.0

# Development Tools (separate file)
# See requirements-dev.txt

# Known Issues:
# - EasyOCR requires CUDA 11.x for GPU support
# - PyMuPDF may require additional system libraries on Linux
# - ONNX Runtime GPU requires CUDA 11.x and cuDNN 8.x
